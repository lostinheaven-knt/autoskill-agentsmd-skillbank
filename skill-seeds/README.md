# skill-seeds

Read-only seed store.

- This directory is an archive of original skills (full directories preserved).
- Seeds are **never modified** by skill evolution.
- Evolution outputs live in `SkillBank/`.

Current layout:
- `openclaw-workspace-skills/` (copied from an OpenClaw workspace skills directory)
