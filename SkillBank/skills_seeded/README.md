# Seeded skills (copied)

This folder is a *copy* of skills from `/root/.openclaw/workspace/skills` for indexing experiments.

- Source workspace is **read-only** for this project: do not modify it.
- Each leaf is copied as:
  - `SkillBank/skills_seeded/<bucket>/<skill_dir>/SKILL.md` (copied from `SKILL.md`)

If you want these to become first-class leaves, manually curate/move them into `SkillBank/skills/`.
